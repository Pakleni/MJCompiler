program p
const int size = 10;
int pace, ogi[];
bool pace_bool, gioBool;
char karakter;
{
    void f(char ch, int a, int arg, int pace=2) int x;
    {
        x = arg;
        print(-pace+1);
    }

    void main() int x, i; char c;
    {
        ogi = new int [size];
        
        read(x);

        if(x > 5)
        {
        	pace_bool = false;
        	c = 'c';
        }

        i = 0;
        
        do{
         i++;
         print(-i);
        }while(i<size);

        f(c, x, i);
        f(c, x, i, 3);

        do{
			if(x <= 0) break;
			if (0 <= x && x < size)
			{
		    	print('a');
		    }
		    else if (-size < x && x < 0)
		    {
		    	print('b');
		    }
		    read(x);
		}while(true);
    }
}